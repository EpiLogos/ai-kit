name: Knowledge shared service helper

on:
  push:
    branches:
      - agent/knowledge-navigation-production-closure

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/knowledge-navigation-production-closure
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: python3 scripts/knowledge_shared_service_patch.py
      - run: |
          rm scripts/knowledge_shared_service_patch.py
          rm .github/workflows/knowledge-shared-service-helper.yml
          cargo fmt --all
          allowed='^(crates/aikit-core/src/knowledge_navigation.rs|crates/aikit-cli/src/app/knowledge.rs|crates/aikit-tui/src/backend.rs|crates/aikit-cli/src/app/mod.rs)$'
          while IFS= read -r file; do
            if [[ "$file" =~ ^crates/ ]] && [[ ! "$file" =~ $allowed ]]; then
              git checkout -- "$file"
            fi
          done < <(git diff --name-only)
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "feat(knowledge): expose production faculty on shared backend"
          git push origin HEAD:agent/knowledge-navigation-production-closure
      - run: cargo check -p aikit-core -p aikit-store -p aikit-tui -p aikit-cli
      - run: |
          cargo test -p aikit-core knowledge --lib
          cargo test -p aikit-store knowledge_application --lib
