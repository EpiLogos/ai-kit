# AIKit #158 — QL-shaped Wiki / Flow return receipt

**Status:** implementation/evidence receipt; not QL semantic authority  
**Owner:** AIKit #158 / PR #160  
**Returned implementation head before this receipt:** `1c800bab028f302be4f3c736d337e48dc12731d9`  
**Acceptance run:** `33667699656`  
**Upstream QL-MEF main merge:** `230ea85d732fd8d6f434767a495e5bdb02c36cc4`

This receipt records the returned AIKit implementation state. QL-MEF remains authoritative for the canonical QL shape contract and deeper Vāk/MEF semantics; AIKit owns the deterministic client-side structural operation over SemanticWiki, Living Knowledge, Flow and Contemplate.

## Accepted composition

```text
FlowRef @ exact source revision
        ↓
standing Flow Context + existing Living Knowledge preflight
        /
materially relevant Wiki Frame / constellation
        ↓
portable QL shape disclosure
        ↓
explicit Contemplate(FlowRef) exactly once
        ↓
relationally generated IntegrativeWikiReading
        ↓
exact bases + Frame + operator + Return
        ↓
generated whole can become later basis
```

The implementation composes the existing apertures rather than adding another cognition system:

- `ql_shaped_flow_contemplate_preflight` combines `FlowContemplatePreflight` and `QlShapedContemplatePreflight` and requires that both disclose the same bounded Living Knowledge field;
- `explicit_ql_shaped_flow_contemplate` enters the already-existing `explicit_flow_contemplate` aperture exactly once through a typed adapter;
- deterministic shape preflight never invokes an Agent/model automatically;
- ordinary Wiki and Flow operation require no live QL-MEF service;
- the generated reading remains Agent knowledge and never rewrites authored Flow source or promotes itself to human Ground.

## Etymological / conceptual Flow specimen

The acceptance specimen uses an actual Flow revision containing the working thread:

`criterion / distinction; retain the difference and ask what their relation determines`

That Flow is a real coordinate in the active Wiki constellation. The shaped preflight exposes the corresponding relational sixfold, and explicit contemplation generates `wiki:reading:criterion-through-distinction` with:

- exact Flow Ref, source Ref and revision retained as basis;
- the materially present conjugate retained as the second basis;
- reversible Return paths from **both** relational poles through the active anchor to the generated whole;
- exact QL shape contract, relational-sixfold shape Ref, generation-site operator, Frame Ref, basis Refs and generation position retained as attribution;
- no Flow mutation and no human-source proposal;
- successful later use of the generated reading as a new integrative basis without losing the original derivation extension.

The first focused run correctly rejected a one-sided Return because Living Knowledge requires every integrative basis to retain a reversible path to the reading whole. The accepted implementation preserves Return from both poles rather than weakening that invariant.

## Verification

Run `33667699656` passed:

1. the etymological Flow → shape → generated whole → source Return specimen;
2. complete-constellation 4×4 / 6×6 / relational-sixfold shape disclosure;
3. relational-generation attribution without authored-Ground promotion;
4. the pre-existing Flow bounded-context / exactly-one-invocation invariant;
5. full workspace `cargo check --workspace --all-targets`;
6. full workspace clippy with `-D warnings`;
7. full workspace tests.

The temporary acceptance workflow and migration scripts removed themselves after the green run; `1c800bab028f302be4f3c736d337e48dc12731d9` is the returned product implementation.

## Current-main reconciliation and PR return

Before merge, PR #160 was reconciled with then-current AIKit `main` at `4e35f499c50b987551ab124b4432757973e823ae`. The reconciled product tree at `cc110e70a95c9397c4a7d03dc498333371553abe` passed all four ordinary PR lanes:

- CI — run `33676165400`;
- Native Skills — run `33676165430`;
- Pre-local release — run `33676165416`;
- Persistent Surface conformance — run `33676165693`.

GitHub's connected ready-for-review mutation was unable to complete because its GraphQL response selection requested a repository field no longer present in GitHub's schema. A one-shot repository-native transport shim therefore performed only `gh pr ready 160`, then removed itself. The shim contained no product logic and its retirement restored the PR to the six owned implementation/receipt files.

The shim-retirement commit was GitHub-Actions-authored, causing the ordinary PR workflows on that commit to require an external approval instead of executing. This human-authored receipt amendment exists only to return the exact clean PR tree to the repository's normal PR-triggered admission path; it does not alter the accepted product semantics above.

## Return

The Wiki/Flow field can now do the operation #158 required:

```text
leaf / materially present complement
        ↓ within actual Frame
relational determination
        ↓
generated whole
        ↓
recursive later basis
        ↓
Return to every exact originating basis
```

`4×4`, `6×6` and the six relational-generation sites remain sparse structural accounting/generation forms. Their coordinates are places at which a relation can be asked or recorded; they do not manufacture semantic truth. Deeper QL/MEF projection remains in QL-MEF.
