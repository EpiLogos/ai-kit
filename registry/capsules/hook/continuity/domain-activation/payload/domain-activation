#!/bin/sh
# The composition handle for the domain-activation engine reaction (W1).
#
# The engine (aikit-cli dispatch) resolves the tuning at event time; when
# this capability is composed it activates declared KnowledgeDomains whose
# triggers match the prompt. This payload only proves the chain reached it:
# exit 0, no output, because hook-chain stdout is a gate channel, not an
# injection channel.
exit 0
