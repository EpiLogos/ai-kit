#!/bin/sh
# The composition handle for the turn-ledger engine reaction.
#
# The engine (aikit-cli dispatch) resolves the continuity tuning at event
# time; when this capability is composed the engine injects the ledger line.
# This payload only proves the chain reached it: exit 0, no output, because
# hook-chain stdout is a gate channel, not an injection channel.
exit 0
